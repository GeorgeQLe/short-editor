# Post-Sync Actions

## Dependencies

```sh
npm install
```

## Notifications

- `package.json` — review dependency or script changes
- `package-lock.json` — dependencies may need reinstalling
- `resources/worker/requirements.lock` — worker dependencies may need rebuilding
