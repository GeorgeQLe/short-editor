# UI-01.4 macOS Electron UAT

Date: 2026-07-30
Platform: macOS, Electron development build
Fixture: isolated credential-free data directory under `/tmp`

## Walkthrough evidence

- Opened the persisted Short and confirmed the fifth **Render** tab was keyboard/accessibility
  visible.
- Approved revision 1 and observed the canonical response update the open project to approved
  revision 2.
- Removed the fixture source, ran preflight, and observed one blocking `SOURCE_MISSING` error
  alongside four advisory `CAPTION_SAFE_AREA` warnings. **Start render** remained disabled.
- Used **Open Library for relinking**, restored the fixture source, reopened the Short, and reran
  preflight. It passed with zero errors and four warnings; **Start render** became available.
- Started the exact passing snapshot with the default SRT sidecar. Attempt 1 appeared immediately
  with an accessible progress element, revision, state, stage, encoder version/codecs/settings,
  pending validation, and pending determinism.
- The synthetic 20-second fixture reached 3% and failed with `VALIDATION_ERROR` because the local
  FFmpeg process exited with code 8 before cancellation could be exercised.
- Retried attempt 1 from its persisted snapshot. Attempt 2 was created in the same lineage and
  attempt 1 remained visible as superseded and ineligible. The fixture hit the same FFmpeg failure,
  so the cancel-to-success portion of the walkthrough remains incomplete.
- Quit and restarted Electron against the same isolated data directory. Reopened the project and
  confirmed both attempts, their failure details, revision, encoder provenance, progress, and retry
  eligibility were restored automatically.

## Automated verification

- Full Vitest suite: 48 files, 346 tests passed.
- Production build and TypeScript checks passed.
- Electron preload smoke passed with 11 exposed functions.
- Release-interface contract tests passed as part of the full suite.
- `git diff --check` passed.

The repository's real FFmpeg composition and determinism tests passed. The manual fixture failure
above is retained as an explicit UAT limitation and was not treated as a successful cancellation or
render.
